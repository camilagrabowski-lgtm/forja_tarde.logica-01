function respostas(){
    var nome = (document.getElementByld(nome)).value
    var idade = parseFloat(document.getElementByld(idade).value)
    var qual = (document.getElementByld(qual)).value
    var raca = (document.getElementByld(raca)).value
    var nacionalidade = (document.getElementByld(nacionalidade)).value
}